import Contact from "../components/Contact";

/** /contact — форма заявки (валидация, состояния отправки). */
export default function ContactPage() {
  return (
    <div className="pt-20 md:pt-24">
      <Contact />
    </div>
  );
}
